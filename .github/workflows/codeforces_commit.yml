name: Codeforces Auto-Commit
on:
  # 🚫 Disabled automatic triggers
  # schedule:
  #   - cron: "*/15 * * * *"  # Disabled auto-run
  workflow_dispatch:  # ✅ Only runs manually
env:
  CF_HANDLE: https://codeforces.com/profile/Samarthkh_17
jobs:
  fetch-and-commit:
    if: false  # 🚫 Prevent job from running at all
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Do nothing
        run: echo "Auto commit temporarily disabled."
